# OBSOLETE (SZP-70): monolit zastąpiony dwoma plikami — bezpieczny copy-paste.
#
# Używaj zamiast tego:
#   deploy/nginx/ekstrabet-limits.conf.example  → /etc/nginx/conf.d/
#   deploy/nginx/ekstrabet-site.conf.example    → sites-available/
#
# Nie kopiuj tego pliku do sites-available: wcześniejsza wersja zawierała
# limit_req_zone poza kontekstem http {}, co psuło `nginx -t`.
